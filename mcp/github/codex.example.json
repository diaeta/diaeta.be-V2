{
  "mcpServers": {
    "github": {
      "command": "mcp-server-github",
      "args": [
        "--port",
        "5181"
      ],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}

